import { Meta, ColorPalette, ColorItem } from '@storybook/addon-docs/blocks';

import { defaultTheme, darkTheme } from '../utils';

<Meta title="Design System/Colors" />

# Colors

<ColorPalette>
  <ColorItem title="theme.color.primary" subtitle="Coral" colors={[defaultTheme.primaryColor]} />
  <ColorItem title="theme.color.secondary" subtitle="Coral" colors={[defaultTheme.inverseColor]} />
  <ColorItem title="theme.color.primaryColorHover" subtitle="Coral" colors={[defaultTheme.primaryColorHover]} />
  <ColorItem title="theme.color.primaryColorACtive" subtitle="Coral" colors={[defaultTheme.primaryColorACtive]} />
  <ColorItem title="theme.color.warning" subtitle="Coral" colors={[defaultTheme.status.warningColor]} />
  <ColorItem title="theme.color.error" subtitle="Coral" colors={[defaultTheme.status.errorColor]} />
  <ColorItem title="theme.color.success" subtitle="Coral" colors={[defaultTheme.status.successColor]} />
  <ColorItem title="theme.color.primary" subtitle="Coral" colors={[defaultTheme.primaryColor]} />
  <ColorItem title="theme.color.primary" subtitle="Coral" colors={[defaultTheme.primaryColor]} />

</ColorPalette>